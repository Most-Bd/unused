const BASE_URL = "https://dummyjson.com/products/category";

const requests = {
  mensShirts: `${BASE_URL}/mens-shirts`,
  mensShoes: `${BASE_URL}/mens-shoes`,
  womensDresses: `${BASE_URL}/womens-dresses`,
  womensShoes: `${BASE_URL}/womens-shoes`,
};

export default requests;
